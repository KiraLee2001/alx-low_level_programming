#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (success/correct)
 */

int main(void)
{
	int p;
	int o;

	for (p = 48; p <= 56; p++)
	{
		for (o = 49; 0 <= 57; o++)
		{
			if (m > n)
				{
					putchar(p);
					putchar(o);
					if (p != 56 || m != 57)
					{
						putchar(',');
						putchar(' ');
					}
				}
		}
	}
	putchar('\n');
	return (0);
}

