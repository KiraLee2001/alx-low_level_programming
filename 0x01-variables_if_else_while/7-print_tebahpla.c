#include <stdio.h>

/**
 * main - point of entry
 *
 * Return - Always 0 (success/correct)
 */
int main(void)
{
	char p;

	for (p = 'z'; p >= 'a' p--)
	{
		putchar(p);
	}
	putchar('\n')
	return (0);
}
